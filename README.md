# Привычки (Habit Tracker)

API-сервис для отслеживания полезных и приятных привычек. Пользователи могут создавать, просматривать и удалять свои привычки, устанавливать периодичность и получать уведомления через Telegram.

## Основные возможности

* **CRUD**-операции для личных привычек.
* **Публичный API** для просмотра списка публичных привычек других пользователей.
* **Сложные валидаторы** для ограничения времени выполнения (не более 120 секунд), периодичности (не реже 1 раза в 7 дней) и правила "Вознаграждение/Связанная привычка".
* **Уведомления Telegram:** Интеграция с Celery для регулярной отправки напоминаний пользователям о необходимости выполнения привычки.
* **Авторизация:** Используется JWT-аутентификация (djoser/simple-jwt).

## 🛠Технологии и требования

* **Язык:** Python 3.13
* **Фреймворк:** Django 5.2.x, Django REST Framework
* **База данных:** PostgreSQL
* **Очередь задач:** Celery с Redis (брокер)
* **Контейнеризация:** Docker, Docker Compose
* **Управление зависимостями:** Poetry

## Запуск проекта с помощью Docker Compose

Самый простой и рекомендуемый способ запустить проект — использовать Docker Compose.

### 1. Подготовка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [ссылка на твой репозиторий]
    cd habit_tracker
    ```
2.  **Создайте файл переменных окружения:**
    Создайте файл **`.env`** в корне проекта (рядом с `docker-compose.yaml`) и заполните его, используя шаблон:

    ```ini
    # .env

    # --- База данных PostgreSQL ---
    DB_NAME=habit_tracker_db
    DB_USER=habit_user
    DB_PASSWORD=secretpassword
    DB_HOST=db
    DB_PORT=5432

    # --- Django ---
    SECRET_KEY=yoursecretkey
    # Название хоста для подключения Celery к Redis
    CELERY_BROKER_URL=redis://redis:6379/0

    # --- Telegram ---
    # Токен бота для отправки напоминаний
    TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_БОТА
    ```

### 2. Запуск

Выполните команду в корне проекта. Она автоматически соберет образ, запустит пять сервисов и выполнит миграции:

   ```bash
      docker compose up --build